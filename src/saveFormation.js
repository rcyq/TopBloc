import React from 'react';

var SaveFormation = React.createClass({

  render: function() {
    return (
      <div className="save">
        <button>
          Save
        </button>
      </div>
    );
  }
});

export default SaveFormation;
